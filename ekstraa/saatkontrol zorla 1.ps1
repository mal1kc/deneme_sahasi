# $height = (Get-Host).UI.RawUI.MaxWindowSize.Height
# $width = (Get-Host).UI.RawUI.MaxWindowSize.Width

# $a = [int](Get-Date -UFormat "%H")
# if ($a -lt 10){
#     shutdown -f -s -t 0
#     Write-Output "erken oturdun saat daha : $a"
# }
# else {
#     $Button = (New-Object -ComObject "WScript.Shell").Popup("Vaktinde Oturdun.", 0, "Helal", 0)
# }

# shutdown -L